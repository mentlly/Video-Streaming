events {}

http {
    server {
        listen 80;
        server_name localhost;

        location /api/auth/ {
            proxy_pass http://auth:3000;
        }

        location /api/stream/ {
            proxy_pass http://go-video-management:8080;
        }
    }
}