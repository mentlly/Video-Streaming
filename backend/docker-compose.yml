services:
  node-api:
    build: ./node-api
    ports:
      - "3000:${NODE_API_PORT}"
    env_file:
      - .env
    volumes:
      - ./node-api:/app
      - /app/node_modules
    #this line for testing
    command: node --watch index.js
